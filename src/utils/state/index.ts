import {
  GlobalStateContext,
  GlobalStateProvider,
  useGlobalContext
} from '../state/GlobalStateProvider';
import { initialState } from './initialState';

export {
  GlobalStateContext,
  GlobalStateProvider,
  useGlobalContext,
  initialState
};
